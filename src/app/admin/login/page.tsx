import { signIn } from "../actions";
/* eslint-disable @next/next/no-html-link-for-pages */
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="grid min-h-screen place-items-center p-4"><section className="card w-full max-w-md p-7"><a href="/" className="text-sm font-bold text-[#b42327]">← Poster studio</a><h1 className="mt-6 text-2xl font-black">Administrator sign in</h1><p className="mt-2 text-sm leading-6 text-black/55">Manage approved Tamil content, branding, templates, and download activity.</p>{error&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-800">{error}</p>}<form action={signIn} className="mt-6 space-y-4"><label><span className="label">Email</span><input className="field" name="email" type="email" required autoComplete="email"/></label><label><span className="label">Password</span><input className="field" name="password" type="password" required autoComplete="current-password"/></label><button className="w-full rounded-xl bg-[#171411] px-4 py-3 font-bold text-white">Sign in securely</button></form></section></main>}
